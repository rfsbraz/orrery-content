stage: translation
locale: pt-PT
summary: >
  Coverage before: 378/399 files, this wing showing MISSING on works.yaml
  (52), events.yaml (6), authors/pedro-sousa-tavares.yaml (1),
  authors/sophia-de-mello-breyner-andresen.yaml (30), and franchise.yaml
  PARTIAL 6/7 (missing description). Coverage after: 383/399, this wing
  fully complete, zero regression on the 16 pre-existing partial files
  belonging to other wings. franchise.yaml's existing overlay (written by
  reading-orders for startHere paths, spoiler-fixed by spoiler-audit) was
  read in full, verified against the current base file, and extended with
  only the missing top-level description field - its startHere prose was
  not touched. Wrote four new overlay files from scratch: works.yaml (52
  synopses), events.yaml (3 events, title+description), authors/
  sophia-de-mello-breyner-andresen.yaml (bio + all 14 lifeEvents,
  including the writing-as-politics-1982 epigraph's title+quote+
  description), authors/pedro-sousa-tavares.yaml (bio). Direct quotes
  already in Portuguese in the base file (the 1982 Jornal de Letras
  quote, the Cantata da Paz line, Guilherme d'Oliveira Martins, the PIDE
  file, the Panteao act's citation) were copied as-is rather than
  re-translated, and the redundant English gloss that followed each in
  parentheses was dropped rather than carried into the overlay. One
  diacritic correction: the base event copy read "Nao podemos ignorar"
  (missing til) for the Cantata da Paz line; written correctly as "Não
  podemos" in the overlay, since it is a direct quotation. Register:
  checked throughout for pt-BR tells (progressive gerunds, quadrinhos/
  ecra/autocarro-class vocabulary) - none found by grep or by eye.
  validate.py --slug sophia-de-mello-breyner-andresen: 0 errors after one
  real bug found and fixed - my own header comments used a literal
  "[[work:id|texto]]" placeholder to explain the inline-reference
  convention, which validate.py's INLINE_REF regex parsed as a real
  unresolvable reference (type=work, id="id"). Reworded every such
  comment to "work:<id>|texto" prose form, outside double brackets, so it
  documents the convention without tripping the resolver. All actual
  [[work:...]] / [[author:...]] / [[franchise:...]] references inside
  translated prose resolve to real ids in this wing.
verified:
  - "python3 scripts/validate.py --slug sophia-de-mello-breyner-andresen: OK, 0 errors (251 pre-existing warnings on other wings hidden by --slug, none of them this wing's)."
  - "python3 scripts/validate.py (unscoped): grep for sophia/pedro-sousa-tavares in the output returns nothing - this wing contributes zero warnings to the catalogue-wide run."
  - "python3 scripts/i18n_coverage.py --slug sophia-de-mello-breyner-andresen: pt-PT 383/399, this wing's five target files (franchise.yaml, works.yaml, events.yaml, both author files) all fully covered, no PARTIAL/MISSING entry for this wing remains."
  - "yaml.safe_load on all five touched/created overlay files: all parse, correct list lengths (franchise.yaml 1 entry, works.yaml 52, events.yaml 3, sophia author 1, pedro-sousa-tavares 1)."
  - "grepped the five files for em dashes and the pt-BR discriminator list in the skill (esta lendo, quadrinhos, roteiro, tela, onibus, banheiro, celular, arquivo, progressive estar+gerund): zero hits."
notTranslated:
  - "eras.yaml: not an overlay gap - the base file is an empty list ([]), no eras defined for this wing (documented in its own header: no received periodisation found), so there is nothing to translate and i18n_coverage.py correctly does not list it."
  - "orders.yaml, theme.yaml, editions.yaml: not prose-bearing files this script tracks (orders.yaml is present but empty for this wing; editions.yaml deliberately carries no overlay per SCHEMA - editions are facts, not translation)."
forStages: {}
