# Pipeline runs

One row per `/author` run, written by `pipeline-audit` as its last act. One run
tells you which stage was expensive *that time*; three tell you which stage is
*always* expensive, and only the second is worth rewriting a skill for.

| date | wing | works | tokens | calls | stages | largest finding |
|---|---|---|---|---|---|---|
| 2026-07-26 | john-shirley | 84 | 2,494,974 | 679 | 11 | `visual-metadata` at 170 calls / 1,942 tok per call, the death-by-fetches shape, on a stage whose skill already says to batch. Two footprint violations: `franchise-research` wrote `theme.art`, `press-archaeology` wrote the art grammar onto 8 events. |
| 2026-07-27 | anna-sewell | 1 | 1,790,368 | 458 | 10 | `lookup.py --author` returned 1911 contaminated candidate rows across "52 works", inflating both `editions` (74 calls for 3 editions) and `visual-metadata` (66 calls for 3 images). Second consecutive wing hitting it. Zero footprint violations. |
| 2026-07-27 | jk-rowling | 37 | 3,063,563 | 1,201 | 13 (+3 follow-ups) | Cross-book spoiler leaks found by fix-cascade, not audit: wing-audit (opus/xhigh, reads the whole wing end to end) flagged only Order of the Phoenix; Half-Blood Prince surfaced only because the OotP follow-up noticed it in passing, requiring a third agent dispatch to sweep the series (121,519 tokens / 48 calls across the two follow-ups). spoiler-audit's own opus/high pass had explicitly logged both books as "confirmedSafeUnchanged". Propose wing-audit's brief require sweeping sibling entries for the same failure-mode pattern the moment one instance is found, rather than flagging the single instance and letting a follow-up discover the sweep was needed. Zero footprint violations (clean sequencing on all three known collision pairs: event-resonance/reading-orders, spoiler-audit/visual-metadata, art-rotation/visual-metadata); zero pipeline-narration-in-comments violations. |
| 2026-07-28 | edgar-allan-poe | 145 | 2,572,869 | 905 | 13 | wing-audit (opus/xhigh) caught the run's real catch: The Murders in the Rue Morgue's cover printed The Tell-Tale Heart's own redacted closing line in gold type under the title, and The Tell-Tale Heart's own cover illustrated the exact floorboard-and-corpse reveal its synopsis was redacted to withhold - caught by downloading and looking at the images, not by resolving URLs, fixed pre-merge by the orchestrator (4c14652) rather than a further agent dispatch. Also confirmed: `reading-orders` (5469967) and `spoiler-audit` (731e7af) both wrote pt-PT prose directly into `content/i18n/pt-PT/franchises/edgar-allan-poe/{franchise,orders}.yaml` - `translation`'s declared sole footprint (`content/i18n/<locale>/** only`) - 75 then 10 lines; zero rework resulted only because `translation`'s own pass (43039d6) checked the overlay against the redaction commit before writing rather than assuming it stale. No other footprint violations found across all 13 stages; every triggered stage (including `event-resonance`'s skip) left a record. |
| 2026-07-28 | miguel-de-cervantes | 8 | 2,076,399 | 800 | 13 | wing-audit (opus/xhigh) caught two BLOCKING factual errors no earlier stage's cross-check found, despite each fact being individually sourced in its own file: press-archaeology's captivity lifeEvent (bfbdcbb) wrongly credited El cerco de Numancia to the Algiers captivity alongside El trato de Argel, when only the latter draws on it; and the bio, era and lifeEvent each gave the 1592/1597 imprisonments a different cause. Both harmonized post-merge by the orchestrator directly on the wing-audit branch (b775834), both locales, alongside two smaller tightenings (07a2c28 comment-sanctuary cleanup; f571d27 fixed reading-orders' own order-of-magnitude slip - "decades" for a 168-year gap - and a spoiler-safety verb). Zero footprint violations found across all 13 stages (one trivial exception noted: press-archaeology's bfbdcbb also fixed a 1-line dead source URL in works.yaml, outside its declared footprint but immaterial); every triggered stage left a commit-level record, including wing-audit itself, which has no handoff file and is recorded only in its own fix commits. |
| 2026-07-28 | robert-bryndza | 25 | 2,489,107 | 963 | 13 | Corrections don't propagate sideways: `press-archaeology` fixed "sold a million copies within five months" to "within a year" in 3 files but never touched `eras.yaml`, which had independently written the same wrong figure from the same About-page source one stage earlier - only `wing-audit` caught the drift. Separately, `editions`' synopsis-match notes reintroduced 3 details `spoiler-audit` had deliberately cut (devils-way's near-drowning, coco-pinchard-the-consequences's remarriage, chasing-shadows's identity), because `editions` runs after `spoiler-audit` and never reads its rewrites - inert today (`edition.note` renders nowhere) but a live trap. Zero footprint violations; opus spend (`eras`, `spoiler-audit`, `wing-audit`) matched real catches. |
| 2026-08-02 | jose-rodrigues-dos-santos | 34 | 2,926,351 | 1,048 | 13 | Footprint violation, second occurrence: `reading-orders` (4ed295a) and `spoiler-audit` (fd80be9) again wrote pt-PT prose directly into `content/i18n/pt-PT/franchises/jose-rodrigues-dos-santos/{franchise,orders}.yaml` (144 + 5 lines) - the same boundary crossing confirmed on edgar-allan-poe (2026-07-28), still not fixed in either skill. Zero rework resulted only because `translation`'s pass (f60d23a) never touched `orders.yaml`, trusting the overlay was already current - the violation is real even though this run got away with it. `visual-metadata` cost 393,160 tokens over 239 calls (1,645 tok/call, textbook death-by-fetches, worse than John Shirley's 170-call baseline on the same stage) despite its skill's `lookup.py`-first instruction; compounded by a tooling gap, not a stage failure - the orchestrator's Agent tool exposes `model` but no `effort` parameter, so the stage's specified low effort could not actually be set. `event-resonance` ran sonnet/medium against a haiku/medium spec (deliberate deviation, flagged by the orchestrator); no same-wing haiku baseline exists to size the delta. Two content errors slipped past `wing-audit` (opus/xhigh) and were fixed by the orchestrator directly afterward (bf4b32d): a misquoted historian in EN `events.yaml` that `translation` had already independently corrected in pt-PT without the fix ever propagating back to EN - the robert-bryndza sideways-propagation failure, third confirmed occurrence - and a wrong character-path count in `orders.yaml` ("three paths" vs "two") that `reading-orders` had gotten wrong identically in both locales. |
| 2026-08-03 | hp-lovecraft | 111 | 3,259,958 | 1,025 | 14 | The pt-PT footprint violation confirmed on edgar-allan-poe and jose-rodrigues-dos-santos did **not** recur a third time - only `translation` (5bcf99a) and the orchestrator's own post-audit fix (ed3d1e4) touched `content/i18n/pt-PT/**`; every other stage stayed in its lane. The run's real finding is a new shape, not that one: twice, unprompted, an earlier stage silently produced a downstream stage's declared deliverable with no handoff record of having done so. `franchise-research` (814b38a) pre-wrote `theme.yaml`'s `art:` block - `visual-language`'s job - and its own handoff records the theme preset but never flags the art block as already done. `press-archaeology` (d907ee6) pre-wrote every life/franchise event's `organisation`/`illustration_type`/`images_required` grading - `art-rotation`'s job - and its handoff self-critiques one organisation choice in a note addressed to `wing-audit` but never tells `art-rotation` the grading is already there. Both were caught only because the owning stage read its input before writing rather than assuming a blank slate, and both independently re-verified the pre-existing work as correct rather than redoing it blind (`visual-language`'s 44312fd: "shipped unchanged rather than rewritten for its own sake"; `art-rotation`'s eb409cd: "independently reviewed all 28... and found it sound"); `wing-audit` (db0ea2a) re-confirmed both correct a third time. No content defect resulted, but neither instance left a record until the owning stage happened to notice - the same "silence only the next reader catches" shape as the spoiler-audit skip finding, now on deliverables instead of on a whole stage. Also worth naming: `visual-metadata` ran 111 calls at 3,200 tok/call (healthy - a real improvement over jose-rodrigues-dos-santos's 239-call/1,645-tok/call death-by-fetches on the same stage, though nothing in the git record confirms whether `lookup.py` is the reason). Total 3,259,958 tokens / 1,025 calls across 14 stages matches the orchestrator's own running table exactly; top three by tokens are `translation` (356,257), `visual-metadata` (355,163) and `editions` (320,055), 31.6% of the run. Orchestrator also made one cosmetic direct fix mid-run (de8ab20: quoted a colon-breaking value in the eras handoff so `validate.py` could parse it) and three direct content fixes after wing-audit (ed3d1e4), all evidenced, none re-litigating a stage's judgement. |
| 2026-08-03 | jrr-tolkien | 32 | 3,006,740 | 1,067 | 13 | A catalogue-wide tooling gap, not a wing defect: `art_rotation.py`'s own `wing_assets()` (scripts/art_rotation.py:276) collects lifeEvents by scanning each work's `authorIds` only - it never reads `withAuthorIds`, and never reads `franchise.yaml`'s `authorIds` either. Christopher Tolkien sits on 13 of this wing's 32 works only via `withAuthorIds` (works.yaml never gives him a bare `authorIds` credit), so his 12 lifeEvents are invisible to the stage's own `--check` gate. Confirmed by reading the script directly, then independently corroborated by `art-rotation`'s own commit (79bce7a), which had to grade Christopher's 12 lifeEvents by hand against "the true single river the app actually renders" - explicitly distinct from "the isolated art_rotation.py --check gate (17 events)" that cannot see them. The earlier fix that added Christopher to `franchise.yaml`'s `authorIds` (9366d4c, event-resonance) does not close this: that field feeds `aura_density.py`/`wing_digest.py` per its own comment, but `wing_assets()` never reads `franchise.yaml` at all, so the two fixes don't overlap. Same shape independently found on robert-jordan and lee-child per the orchestrator (not re-verified here) - three wings now hitting a gap that only bites multi-author wings, which this catalogue keeps adding. Fix proposed: `wing_assets()` should union `authorIds` and `withAuthorIds` when collecting per-work author credits, the way `enrich.py:46` already does (`validate.py:709` checks the two fields separately - it confirms each `withAuthorId` resolves to a real author, but never merges them for a credit scan, so it is not itself a precedent for the union, only proof the catalogue already treats the two fields as parallel). Elsewhere the run was clean: sideways-propagation held (Gilson/Smith's WWI deaths and the LOTR gestation-years correction both landed in both locales in the same wing-audit commits - 0b030ad, e4961e5 - and pt-PT's own prose now reads "doze anos", the corrected figure, confirmed by reading the file); the pt-PT-boundary violation confirmed on edgar-allan-poe and jose-rodrigues-dos-santos did not recur a second consecutive time (`reading-orders` d36b2bd and `spoiler-audit` c3985ff both touched EN-only files), though no skill edit is on record explaining why it stopped. `world-events` was skipped on evidence (`event_density.py` showed no decade in this author's span is empty in `global.yaml`) and left a record. `validate.py --slug jrr-tolkien`: 0 errors, 3 non-blocking warnings (three posthumous/reissue works - 2021, 2024, 2025 - fall outside the wing's four era date ranges, which is expected for a career whose eras are drawn from the author's own lifespan). |
| 2026-08-03 | john-grisham | 58 | 2,693,002 | 953 | 13 | Self-reported verification claims don't hold up under wing-audit's own re-derivation - but not on the stage the run's framing pointed at. `completeness-auditor` (b55db55, "verified all 58 works... against primary/publisher sources") owns **none** of wing-audit's 5 BLOCKING findings; all five belong to stages that ran *after* it and touched files it never wrote (`filesWritten: works.yaml` bibliography prose only; `filesNotTouched: franchise.yaml, theme.yaml`, both author files). The real misses are `editions` (4f506e1, claimed "every ISBN-13 check digit independently recomputed... catching nine same-year Limited/Signed/Large Print traps") missing a tenth - The Client's 758pp Large Print ISBN (9780385468657) shipped as its 401pp Doubleday trade first edition - and separately asserting a "genuinely unrecoverable" ISBN for A Time to Kill's 1989 Wynwood printing that one OpenLibrary fetch resolves (9780922066032, OL2187616M); and `visual-metadata` (5fb7232, "every cover URL fetched and confirmed live... 12 first-pass candidates rejected") self-contradicting its own handoff, which logged the-street-lawyer's "Save £1" sticker cover (10692917) as an already-rejected candidate while that exact URL shipped in `works.yaml`, plus two false "no standard trade cover exists" comments (Boys from Biloxi, Camino Ghosts) where a clean jacket sat on an ISBN the wing's own `editions.yaml` already cited. completeness-auditor isn't blameless - it owns 2 of wing-audit's non-blocking findings, both in its own territory (an essay title `editions` had already found in `notableFinds` and asked the curator to fold in, never folded in; a "forty-plus legal thrillers" framing the wing's own tiering contradicts) - but the shape is domain-scoped self-overclaiming per stage, not one weak stage contaminating the next. Positive control: the genre-count routing fix (3c55d04) landed in EN (`franchise.yaml`, `theme.yaml`, `works.yaml`, `authors/john-grisham.yaml`) and pt-PT (`content/i18n/pt-PT/franchises/john-grisham/franchise.yaml`) in the same commit, so the sideways-propagation failure seen on robert-bryndza and jose-rodrigues-dos-santos did not recur here either. `validate.py --slug john-grisham`: OK, 0 errors, 58 works, full pt-PT. |
| 2026-08-04 | dan-brown | 10 | 895,876 (5 of 15 stages only - see note) | 367 (same caveat) | 15 (franchise-research, completeness-auditor, press-archaeology, eras, event-resonance, reading-orders, spoiler-audit, visual-language, art-rotation, visual-metadata, editions, translation, wing-audit, audit-fixes, pipeline-audit; `world-events` not triggered) | **Cost gap, stated honestly rather than guessed**: stages 1-7 (`franchise-research` through `art-rotation`) ran in a prior session this orchestrator wasn't present for, so no per-stage token/call numbers exist for them - not zero, missing. Git timestamps are a weak proxy at best (scaffold to `visual-language`'s handoff spans 69 minutes over 13 commits for 7 of those 9 skills; `art-rotation`'s own gap is contaminated by what looks like an overnight break) and are not presented as measured cost. The only clean signal: this session's 5 measured stages (`visual-metadata` finish+verify, `editions`, `translation`, `wing-audit`, `audit-fixes`) total 895,876 tokens / 367 calls, `wing-audit` (opus) the largest at 236,612/81. **Zero footprint violations** on all three named collision pairs, confirmed by diff, not just by commit order: `event-resonance` (6ea164e) wrote only `globalEvents`, `reading-orders` (5dda932) only `startHere` + `orders.yaml`, sequential as required; `spoiler-audit` (f085101) rewrote prose fields in `works.yaml`, `visual-metadata` (12771f2) added only `images` fields there, sequential; `art-rotation` (6aa5883) added one `modifier` line to `authors/dan-brown.yaml`, `visual-metadata` (12771f2) added only the `images` block there, sequential. `translation` stayed inside `content/i18n/pt-PT/**` throughout - the edgar-allan-poe/jose-rodrigues-dos-santos violation has now gone two runs without recurring. **A third confirmed occurrence of the hp-lovecraft pre-write pattern**, this time not silent: `press-archaeology` (ee2a057) set `organisation`/`illustration_type`/`images_required` on all 9 events and lifeEvents while building the biographical spine - `art-rotation`'s declared fields - but its own handoff undersold the scope, naming only "layout fields on the two pre-existing lifeEvents" when the diff shows all 9 got them. `art-rotation` (6aa5883) caught the gap by reading its input rather than assuming a blank slate, reviewed and concurred with all 9 placements against LAYOUT.md, and this time documented the pre-write explicitly in its own commit message rather than leaving it for `wing-audit` to notice - the record hp-lovecraft's occurrences lacked. No rework resulted. Separately, `wing-audit` (opus/xhigh) caught two blocking defects and opened a cross-stage dependency gap no single stage owns: the pt-PT overlay-drift and sideways-non-propagation shapes seen on robert-bryndza/jose-rodrigues-dos-santos did **not** recur, but `press-archaeology`'s own aura-hole count (reported as 4 years, 2012-2015) went stale the moment `event-resonance` excluded globals that had been covering 2007-2011, turning it into a 9-year hole nobody re-measured until `wing-audit` did - not a footprint violation (both stages wrote only their own fields correctly) but a dependency the aura metric has on a later stage's ruling that no stage currently re-checks. Closed same-session by a direct `audit-fixes` pass (6342aff), not a stage re-dispatch. Four systemic (non-content) findings routed to the ledger below; #12 also opened as orrery#161. |

## What two runs already show

**Cost is not shelf-size-sensitive.** A 1-work wing cost **72%** of an 84-work
wing. No stage came near the ~1.2% a proportional cost would predict; the
best-scaling one, `editions`, still cost 58% while making *more* calls.

**Two stages are anti-correlated with wing size** - they cost *more* on the
smaller wing:

| stage | john-shirley | anna-sewell | tokens | calls |
|---|---|---|---|---|
| `visual-language` | 137,438 / 17 | 157,127 / 35 | +14% | +206% |
| `art-rotation` | 149,668 / 21 | 163,455 / 42 | +109% | +200% |

The evidenced mechanism, from `visual-language`'s own run record, is a
catalogue-wide sweep: it checked its `lineCharacter` "against all 19 other
wings". That cost scales with the **catalogue**, not the wing, so it grows on
every future wing no matter how small. `art-rotation`'s doubling has the same
shape but is not yet confirmed to the same standard.

This is the sharpest thing the ledger has produced, and it is only visible
across runs - a single run would have read those two as ordinary mid-cost
stages.

**Skipping well is cheaper than running everything.** The Sewell run skipped
three stages on judgement and one silently. `eras` was the largest single
saving of the run - written directly because a one-book career cannot have
creative periods, which is arithmetic rather than discovery, against 195,614
tokens when the same stage ran as an agent on Shirley. That saving came from
the orchestrator's own initiative and is not yet written into any skill.

## Findings, and what was done about them

Eleven findings came out of the two runs. All eleven are now closed, seven of
them by changing a skill or a script rather than by asking anyone to remember
something.

| # | finding | outcome |
|---|---|---|
| 1 | `visual-language` reads every wing's `theme.yaml` to place itself against the catalogue - a cost that grows with the catalogue, not the wing | `scripts/theme_digest.py`: the collision axes in ~8.5KB against ~126KB, then read in full only the wing it shows you are adjacent to |
| 2 | Eleven catalogue-wide validations in one wing build where ten could have been scoped | `--slug` bullet in the shared tooling block now carries the measured cost |
| 3 | `visual-metadata` sources `images.header` into `franchise.yaml`, which nothing read | Kept, and the app now renders it (orrery#131). The stage was right and the app was behind |
| 4 | `eras` cost 195,614 tokens as an agent on an 84-work wing and ~0 written directly on a 1-work one | `author.md` bypasses the stage when the shelf is one or two works, where the answer is arithmetic |
| 5 | `translation` is wing-scoped but `world-events` appends to a shared file, so a run can leave `global.yaml` short a locale | `translation`'s scope now explicitly includes any shared file the run touched |
| 6 | Global events were being written that no author in the catalogue could reach | `world-events` now asks whether any author actually reaches an entry before committing it |
| 7 | A stage skipped without a record is indistinguishable from a stage that failed | `pipeline-audit` gained a silent-skip detector; the silence is the failure, not the judgement |
| 8 | `lookup.py --author` returned 1,911 contaminated candidate rows across two consecutive wings | `by`/`same_author` columns, an `--author-only` flag, and a stderr warning naming the contaminating authors |
| 9 | OpenLibrary's ASCII-only URL construction dropped diacritics and Norwegian titles | Fixed in `providers.py`, with a regression test that captures the provider's real URL rather than rebuilding it |
| 10 | Six wings name a `displayFace` or `signature` the app does not implement, so their argued branding renders as the default | Two implemented (`instrument-serif`, `filament`, orrery#131); the validator's sets now track `lib/theme.ts`. Four remain, blocked on decisions rather than on work |
| 11 | `Franchise.images` was set by 15 wings but absent from the app's interface and rendered nowhere | Declared and rendered, with the credit, since several are CC BY (orrery#131) |

Findings 12-15 came out of the dan-brown run (2026-08-04), routed here by its
`wing-audit` and `pipeline-audit` stages. Unlike 1-11 they are not yet closed -
this ledger tracks them open until each has a fix in place.

| # | finding | outcome |
|---|---|---|
| 12 | `pickEdition` (`lib/content/editions.ts:29-49`) scores by language rank then year, so when two editions of a work share a language the *newer* one always wins the tiebreak - regardless of which is primary. On dan-brown an English reader of The Da Vinci Code is sent to the 2016 Young Adult Adaptation (`9781524715847`) instead of the 2003 Doubleday first edition (`9780385504201`), because both are `language: en` and 2016 > 2003. General: any wing with a verified-but-non-primary edition newer than its canonical one has this, and nothing reports it | Not yet fixed - filed as [orrery#161](https://github.com/rfsbraz/orrery/issues/161); wants a schema field for edition primacy, not deletion of the sourced YA entry |
| 13 | `validate.py`'s comment-policy scanner (`PROCESS_COMMENT`, ~line 1307) matches per line, so a pipeline-narration phrase that wraps across a line break inside a comment block passes clean. Verified on dan-brown: `editions.yaml`'s "...fact this / stage surfaced..." and `theme.yaml`'s "...intentionally left / for the visual-language stage" both shipped green for exactly this reason, caught only when `wing-audit` re-ran the same regex over joined comment blocks and fixed both by hand. Every wing built with the current scanner carries this same untested blind spot | Not yet fixed - needs the scanner to join comment blocks before the `PROCESS_COMMENT` regex runs, not a per-line match. Left as an explicit action item; not attempted here, since a mechanical fix to shared tooling is outside a single-wing audit's footprint |
| 14 | `scripts/aura_density.py` (~line 109) still checks `eid in included or (lo_life <= y <= hi_life)` - the arithmetic-lifetime default `docs/SCHEMA.md` and the app (`lib/content/index.ts:210-212`) retired in favour of an include-only model. Harmless on dan-brown (every in-window global was explicitly ruled either way), but on any wing that leaves one unruled it would silently count an excluded-by-default global as covering the aura when the app would not render it there | Not yet fixed - `aura_density.py` needs updating to the include-only model |
| 15 | `validate.py` (~line 1022) requires `theme.art.emblem`, but `docs/SCHEMA.md`'s example never documents that field as required - spec and implementation have drifted apart | Not yet fixed - either document the requirement in `SCHEMA.md` or relax the validator, whichever is actually correct; not decided here |

**A correction to finding 10 as first written.** It was recorded as the app
discarding branding "with nothing reporting it". That was wrong: `validate.py`
had been warning on all eight values by name, on every run, with the
consequence spelled out. The failure was never detection - it was that the
warning sat among 77 others and no one reads a 77-line list. So the fix is two
things, not one: the sets now track the app, and an unscoped run rolls its
warnings up by kind. The rollup earns its place immediately - **41 of the 77
are a single class** (pipeline narration in content comments), which is more
than half the catalogue's standing debt hiding in plain sight as sixty-odd
unrelated-looking lines.

The general lesson is worth more than the specific fix: a check nobody reads is
not a check, and a long warning list is how a correct check becomes invisible.

**A stage's confidence language is not evidence, even when the stage did real work.**
Three instances now, across two runs and three different stages: jk-rowling's
spoiler-audit (opus/high) logged Half-Blood Prince "confirmedSafeUnchanged" and
the leak was caught only by a follow-up dispatch, not by the audit itself;
john-grisham's editions claimed to have caught "nine same-year Limited/Signed/
Large Print traps" via independently recomputed check digits and missed a
tenth on The Client; john-grisham's visual-metadata logged the-street-lawyer's
sticker cover as a rejected candidate in its own handoff while that exact URL
shipped. In all three, the process description was true (the check really ran)
and the outcome it was cited to support was false. Watching for a fourth
occurrence before proposing a skill change - the fix is not obvious yet
(a checklist a stage fills in itself has the same self-report problem one
level down).
